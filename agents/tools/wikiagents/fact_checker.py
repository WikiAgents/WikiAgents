from shared.models import WikiContextInfo


def fact_checker(wiki_context: dict, comment: str):
    """Use this tool to ground your content and check correctness of the claims"""
    return "All facts checked. Everything seems to be factually correct!"
